{
  "name": "jailbreak-project",
  "version": "1.0.0",
  "scripts": {
    "mock-server": "node mock-server.js",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.5"
  }
}